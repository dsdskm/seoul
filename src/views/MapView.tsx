import { Box } from "@mui/material";

const MapView = () => {
  return (
    <>
      Map View
      <Box sx={{ display: "flex", flexDirection: "column", justifyContent: "center", alignItems: "center" }}></Box>
    </>
  );
};

export default MapView;
